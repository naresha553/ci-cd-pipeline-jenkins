# Maven
app/target/
*.jar
*.war
*.class

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/.terraform.lock.hcl
terraform/kubeconfig

# Jenkins / local secrets
.env
*.log

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Kind / kube
kubeconfig
# Note: terraform/kind-config.yaml IS tracked (needed by the pipeline).
# Only ignore generated configs:
kind-config.generated.yaml
